---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- 桌面 exe 依赖系统已安装 WebView2 Runtime，后续可评估随包提供 Evergreen Bootstrapper 或在缺运行时给出下载引导。
- 桌面构建流程（前端导出 → 复制到 `webui/out` → `go build -tags desktop`）目前是手工三步，后续可整理为单一构建脚本。
- 成片字幕烧录默认使用 Microsoft YaHei 字体，Docker/Linux 部署环境若无该字体会回退默认字体导致中文缺字形，后续可考虑随镜像内置中文字体或支持配置 `fontdir`/字体名。
- 剪映草稿导出当前按公开资料常见格式生成（`draft_content.json` version `360001`），不同剪映版本的草稿格式兼容性还需持续实测跟进。
- AI 漫剧生产线（/drama）的项目数据目前仅存浏览器本地（localforage），登录后云同步漫剧项目为后续待办。
- 百炼渠道后续待办：① `cosyvoice-v2` 等语音合成模型暂未接入（当前仅支持 qwen3-tts 系列）；② 百炼无浏览器直连能力，未登录一律拒绝，后续可评估与匿名代理打通；③ 图生视频首帧目前以 Base64 data URL 内联传输，请求体较大，后续可优化为先上传 OSS 再以 URL 传参；④ 百炼视频完成后的临时 OSS 链接需持久化转存结果回写服务端任务（当前仅当次会话内转存，刷新/过期后重新打开会重复下载或失败）；⑤ /drama 图生视频步骤刷新后前端无恢复逻辑（本地 busy 状态，刷新后需手动重新点击生成）。
- Qoder 通道目前仅 WebSocket 回环传输，后续可评估 SSE / Streamable-HTTP 作为二期候选（改善 https 部署下浏览器拦截本地 ws 的限制）。
- 自部署算力服务器方案已决定放弃、改走云端（方向待定）；原服务器侧二期设想（角色 LoRA 训练、Qwen-Image-Edit Q4 → Q6 量化升档、ControlNet 构图接线）仅作云端方案参考留档。
- comfyui2api 上游缺陷：ComfyUI 报 execution_error 时单 worker 阻塞全队列至 3600s 超时（本轮实测卡队约 1 小时），建议推动上游修复或调低该超时。
- 漫剧视频路径已知问题：① 「生成全部视频」仅触发第一个分镜；② 5B 快档视频时长约 2 秒（工作流 49 帧上限）；③ 刷新/硬导航会静默丢弃在途视频生成（图片路径有中断提示，视频路径无）；④ 离开成片步骤再返回时成片展示区消失（任务记录仍在后端）。
- FFmpeg 退出码 -2 在错误信息中显示为 4294967294 且报错文案指向清单文件，易误判，后续可在错误归一化处优化。
- ComfyUI 自定义参数（IPAdapter 权重 / LoRA 参数等）仅能经 `POST /v1/jobs` 提交，OpenAI 风格端点不透传，后续可评估支持。
